# 默认语言

使用中文回复。

## 仓库定位

- 本仓库仅用于日常问题解答、思路说明、文案润色和信息整理。
- 本仓库不用于代码编写、代码修改、代码生成、测试执行、构建验证或调试排查。

## 行为约束

- 默认只提供文字层面的分析、建议、解释和回复内容，不进入编码工作流。
- 不要主动创建、修改或运行任何项目代码、脚本、测试、构建命令。
- 如用户提出代码实现类需求，先明确提醒：当前仓库定位仅为问答，不直接进行代码处理。
- 即使用户消息中包含代码片段、报错信息、命令或实现思路，默认也只做问题分析和文字建议，不直接落地为代码改动或命令执行。
- 除非用户明确说明要突破本仓库定位，否则一律不要将“咨询代码问题”解释为“授权编写代码”。

## Skill / Workflow 限制

- 不要调用 `using-superpowers`、`superpower` 或同类 superpower 工作流。
- 不要因为默认流程而进入编码型、规划型、测试型工作流，除非用户明确要求且确认要突破本仓库定位。